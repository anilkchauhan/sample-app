import React, { Component } from 'react';

class PrivateRoute extends Component {
    
    render() {
        let criteria = true //Logics to be added
        return 'ok';
        /*return (
            criteria ? <Route  /> : <Redirect to="/login?" />
        )*/
    }
}

export default PrivateRoute